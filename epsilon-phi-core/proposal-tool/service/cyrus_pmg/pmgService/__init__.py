"""Mirror of ``cyrus_pmg.pmgService`` - the FastAPI backend the dashboard
proxies to. ``isgPMGService.py`` is the app; endpoints live on
``dashboardRouter.py`` under ``/api/v1/``; the Proposal Tool back end is the
``scenario`` package."""
